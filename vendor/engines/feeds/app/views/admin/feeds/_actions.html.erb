<ul>
  <% if Admin::FeedsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_feeds_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_feed_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::FeedsController.sortable? and Feed.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Feeds"),
                 admin_feeds_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Feeds"),
                 admin_feeds_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>